# Set to false to run backend without Firebase auth (for local development ONLY)
AUTH_REQUIRED=false

# --- Vertex AI (Gemini) decision brain ---
VERTEX_ENABLED=true
VERTEX_PROJECT_ID=smartaid-ai-490615
VERTEX_LOCATION=us-central1
VERTEX_MODEL=gemini-1.5-flash
VERTEX_TEMPERATURE=0.2
VERTEX_MAX_TOKENS=512
